/*
 * <%= @copyright %>
 */
package <%= @vars.package %>.controller.startup
{
import org.robotlegs.utilities.modular.mvcs.ModuleCommand;
//TODO 需要添加asdoc
public class <%= @vars.module_name %>StartupCommand extends ModuleCommand
{
    //==========================================================================
    //  Constructor
    //==========================================================================
    /**
     * 构造一个新的<code><%= @vars.module_name %>StartupCommand</code>实例.
     * 
     */
    public function <%= @vars.module_name %>StartupCommand()
    {
        super();
    }
  
    //==========================================================================
    //  Methods
    //==========================================================================
    /**
     * @inheritDoc
     */
    override public function execute() : void
    {
        // model

        // view
        
        // controller
    }
}
}