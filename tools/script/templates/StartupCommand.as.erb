/*
 * <%= @copyright %>
 */
package <%= @vars.package %>.controller
{
import org.robotlegs.mvcs.Command;
//TODO 需要添加asdoc
public class <%= @vars.module_name %>StartupCommand extends Command
{
    //==========================================================================
    //  Constructor
    //==========================================================================
    /**
     * 构造一个新的<code><%= @vars.module_name %>StartupCommand</code>实例.
     * 
     */
    public function <%= @vars.module_name %>StartupCommand()
    {
        super();
    }
  
    //==========================================================================
    //  Methods
    //==========================================================================
    /**
     * @inheritDoc
     */
    override public function execute() : void
    {
        // model

        // view
        
        // controller
    }
}
}