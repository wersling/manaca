/*
 * <%= @copyright %>
 */
package <%= @vars.package %>.controller.startup
{
import org.robotlegs.utilities.modular.mvcs.ModuleCommand;
//TODO 需要添加asdoc
public class <%= @vars.module_name %>ShutdownCommand extends ModuleCommand
{
    //==========================================================================
    //  Constructor
    //==========================================================================
    /**
     * 构造一个新的<code><%= @vars.module_name %>ShutdownCommand</code>实例.
     * 
     */
    public function <%= @vars.module_name %>ShutdownCommand()
    {
        super();
    }
  
    //==========================================================================
    //  Methods
    //==========================================================================
    /**
     * @inheritDoc
     */
    override public function execute() : void
    {
        // model

        // view
        
        // controller
    }
}
}