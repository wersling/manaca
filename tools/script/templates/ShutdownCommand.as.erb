/*
 * <%= @copyright %>
 */
package <%= @vars.package %>.controller
{
import org.robotlegs.mvcs.Command;
//TODO 需要添加asdoc
public class <%= @vars.module_name %>ShutdownCommand extends Command
{
    //==========================================================================
    //  Constructor
    //==========================================================================
    /**
     * 构造一个新的<code><%= @vars.module_name %>ShutdownCommand</code>实例.
     * 
     */
    public function <%= @vars.module_name %>ShutdownCommand()
    {
        super();
    }
  
    //==========================================================================
    //  Methods
    //==========================================================================
    /**
     * @inheritDoc
     */
    override public function execute() : void
    {
        // model

        // view
        
        // controller
    }
}
}