## Don't change these settings ###
 
#build locations
asdoc.loc=${FLEX_HOME}/bin/asdoc
main.src.loc=${basedir}/src
test.src.loc=${basedir}/test
doc.loc=${basedir}/bin/doc
exlib.loc=${basedir}/libs
testlib.loc=${basedir}/build/common/libs
bin.loc=${basedir}/bin
report.loc=${basedir}/bin/report
deploy.loc=${basedir}/deploy
template.loc=${basedir}/build/templates